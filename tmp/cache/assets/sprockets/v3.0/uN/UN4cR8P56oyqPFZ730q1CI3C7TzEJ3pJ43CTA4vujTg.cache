I"ã(function() {
  $(document).ready(function() {
    var user;
    user = $("[name='userid']").attr("content");
    return App.mailbox = App.cable.subscriptions.create({
      channel: "MailboxChannel",
      userid: user
    }, {
      connected: function() {
        return console.log("Channel Open");
      },
      disconnected: function() {
        return console.log("Channel Closed");
      },
      received: function(data) {
        console.log("you received data through channel");
        $("#" + msg.chat_id).children(".input_box").val("");
        return $("#" + msg.chat_id).children(".message_box").append("<li class=received_message><p class=message_text> " + msg.text + " </p></li>");
      }
    });
  });

}).call(this);
:ET