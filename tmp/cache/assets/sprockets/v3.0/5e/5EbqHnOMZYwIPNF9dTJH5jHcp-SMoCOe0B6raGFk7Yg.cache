I"Ì(function() {
  var admin_search_users;

  admin_search_users = function() {
    return $("#users_searchbar").keyup(function() {
      var filter, i, input, item, len, li, ul;
      input = $('#users_searchbar');
      filter = input.text();
      ul = $("#admin_users_list");
      li = ul.children();
      for (i = 0, len = li.length; i < len; i++) {
        item = li[i];
        console.log(item.text());
      }

      /*
          a = item.children("a")
          if a.html().toUpperCase().indexOf(filter) > -1
              item.css("display","none")
          else
              item.css("display","")
       */
    });
  };

  $(document).ready(function() {
    admin_search_users();
  });

}).call(this);
:ET