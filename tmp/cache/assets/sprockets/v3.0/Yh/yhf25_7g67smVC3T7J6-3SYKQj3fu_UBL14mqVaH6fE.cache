I"‰(function() {
  var admin_search_users;

  admin_search_users = function() {
    return $("#users_searchbar").keyup(function() {
      var filter, input, ul;
      input = $('#users_searchbar');
      filter = input.text();
      ul = $("#admin_users_list");
      $.each(ul, function(index, value) {
        return console.log(value.html);
      });

      /*
          a = item.children("a")
          if a.html().toUpperCase().indexOf(filter) > -1
              item.css("display","none")
          else
              item.css("display","")
       */
    });
  };

  $(document).ready(function() {
    admin_search_users();
  });

}).call(this);
:ET