I"(function() {
  var admin_search_users;

  admin_search_users = function() {
    var a, filter, i, input, item, len, ref, ul;
    input = $('#users_searchbar');
    filter = input.text();
    ul = $("#admin_users_list");
    ref = ul.children();
    for (i = 0, len = ref.length; i < len; i++) {
      item = ref[i];
      a = item.children("a").first;
      if (a.html().toUpperCase().indexOf(filter) > -1) {
        item.css("display", "none");
      } else {
        item.css("display", "");
      }
    }
  };

}).call(this);
:ET